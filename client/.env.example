# Leave blank only for local Vite-proxy development. Both are required for a
# production Vercel build because Vercel hosts the static client, not the API or Socket.IO server.
VITE_API_URL=
VITE_SOCKET_URL=
# Optional public production URL used only for canonical and Open Graph URLs.
VITE_PUBLIC_SITE_URL=
