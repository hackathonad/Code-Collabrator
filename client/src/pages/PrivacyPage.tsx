import { Link } from "react-router-dom";

export const PrivacyPage = () => <main className="theme-page-home min-h-[70vh] px-4 py-10"><article className="theme-panel mx-auto max-w-3xl rounded-2xl border p-6 shadow-panel sm:p-9"><Link className="text-sm text-[var(--accent)] hover:underline" to="/">← Back to Code Collaborator</Link><h1 className="mt-5 font-display text-4xl theme-text-primary">Privacy</h1><p className="mt-3 text-sm theme-text-muted">This plain-language notice describes the data Code Collaborator uses to provide collaborative coding. It is not a legal certification.</p><section className="mt-7 space-y-5 text-sm leading-6 theme-text-secondary"><div><h2 className="font-semibold theme-text-primary">Accounts and rooms</h2><p>When enabled, Supabase manages sign-in sessions and account/profile data. Room, workspace, chat, history, and file content are used to synchronize the collaborators in that room and may be persisted by the configured backend.</p></div><div><h2 className="font-semibold theme-text-primary">Analytics</h2><p>First-party analytics is designed to use low-frequency activity metadata only. It excludes source code, file contents, chat contents, AI prompts and responses, credentials, audio, video, and shared-screen contents. Analytics is non-critical and may be unavailable.</p></div><div><h2 className="font-semibold theme-text-primary">AI, GitHub, and media</h2><p>AI requests are sent to the selected configured provider. Local Ollama requests may remain local when the backend is local; cloud providers process requests under their own terms. GitHub access is limited to permissions you approve and access tokens are stored server-side when configured. Voice, video, and screen sharing use the configured media service; this application does not claim to record or end-to-end encrypt media.</p></div><div><h2 className="font-semibold theme-text-primary">Guest sessions and choices</h2><p>Guest sessions identify a participant within a room. Avoid placing secrets in shared workspaces. Contact the project operator for deletion or data questions; deployment operators control retention and service configuration.</p></div></section></article></main>;
